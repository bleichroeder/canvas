# Both values are baked into the web bundle at build time.
# The web app uses these as defaults when localStorage is empty,
# so the Tesla browser never needs the Settings page filled in.
VITE_PASSENGER_API=https://passenger-api.<your-account>.workers.dev
VITE_PASSENGER_TOKEN=
